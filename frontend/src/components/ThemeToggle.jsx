import React from 'react';
import { useTheme } from '../contexts/ThemeContext';

export default function ThemeToggle() {
  // Component intentionally disabled to remove the theme icon from the UI
  return null;
}
